name: flutter_boutique
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.1.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # --- NOS DÉPENDANCES ---
  flutter_dotenv: ^5.1.0
  supabase_flutter: ^2.0.2
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # --- FICHIERS ASSETS ---
  assets:
    - .env 